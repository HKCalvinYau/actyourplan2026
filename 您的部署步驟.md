# 🚀 您的部署步驟（已為您準備好）

## ✅ 我已經幫您完成的：

1. ✅ **更新了 `keystatic.config.ts`**
   - 已設定為 GitHub storage
   - 已填入您的 GitHub 資訊：
     - 用戶名：`HKCalvinYau`
     - Repository：`actyourplan2026`

2. ✅ **連接了 GitHub Repository**
   - 已連接到：`https://github.com/HKCalvinYau/actyourplan2026.git`

3. ✅ **提交了所有更改**
   - 所有文件已準備就緒

---

## 📋 接下來您需要做的（3 個步驟）

### 步驟 1: 創建 GitHub Token（約 5 分鐘）

**這是用來讓後台編輯功能工作的：**

1. 打開瀏覽器，前往：https://github.com
2. 登入您的 GitHub 帳號
3. 點擊右上角您的頭像 → **"Settings"**
4. 左側選單，滾動到底部 → **"Developer settings"**
5. 點擊 **"Personal access tokens"** → **"Tokens (classic)"**
6. 點擊 **"Generate new token"** → **"Generate new token (classic)"**
7. 填寫：
   - **Note**: `THE BASE Keystatic`
   - **Expiration**: 選擇 **"No expiration"**（或 90 天）
   - **勾選權限**: ✅ **repo**（在 repo 區塊下）
8. 點擊 **"Generate token"** 綠色按鈕
9. **⚠️ 重要：** 複製這個 Token（只顯示一次！）
   - 看起來像：`ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
   - **請保存好！**（稍後在 Cloudflare 會用到）

---

### 步驟 2: 推送代碼到 GitHub（約 2 分鐘）

**打開終端機（Terminal），執行：**

```bash
cd "/Users/calvinyau/Documents/the base"
git push -u origin main
```

**如果要求輸入帳號密碼：**
- 用戶名：`HKCalvinYau`
- 密碼：**貼上剛才創建的 GitHub Token**（不是 GitHub 帳號密碼！）

**如果成功，您會看到類似這樣的訊息：**
```
Enumerating objects: 82, done.
Writing objects: 100% (82/82), done.
To https://github.com/HKCalvinYau/actyourplan2026.git
 * [new branch]      main -> main
```

---

### 步驟 3: 在 Cloudflare Pages 部署（約 10 分鐘）

#### 3.1 創建 Cloudflare Pages 專案

1. 打開瀏覽器，前往：https://dash.cloudflare.com
2. 登入您的 Cloudflare 帳號（如果沒有，先註冊，免費）
3. 左側選單，點擊 **"Pages"**
4. 點擊 **"Create a project"** 按鈕
5. 點擊 **"Connect to Git"**
6. 選擇 **"GitHub"**（如果第一次使用，需要授權 Cloudflare 訪問 GitHub）
7. 選擇您的 Repository：**`actyourplan2026`**
8. 點擊 **"Begin setup"**

#### 3.2 配置構建設定

在構建設定頁面：

1. **Project name**: `actyourplan2026`（或您喜歡的名稱）
2. **Production branch**: `main`（保持預設）
3. **Framework preset**: 選擇 **"Next.js"**（會自動填充）
4. **Build command**: `npm run build`（應該已經自動填充）
5. **Build output directory**: `.next`（應該已經自動填充）
6. **Root directory**: 留空（不要填任何東西）

#### 3.3 設定環境變數 ⚠️ 重要！

在構建設定頁面，找到 **"Environment variables"** 區塊，點擊展開。

點擊 **"Add variable"** 按鈕，添加以下 **4 個變數**：

**變數 1:**
- Variable name: `ADMIN_USERNAME`
- Value: `admin`（或您想要的管理員用戶名）
- Environment: 選擇 **"Production"**

**變數 2:**
- Variable name: `ADMIN_PASSWORD`
- Value: `您的安全密碼`（例如：`MySecurePass123!`）
- Environment: 選擇 **"Production"**

**變數 3:**
- Variable name: `NODE_ENV`
- Value: `production`
- Environment: 選擇 **"Production"**

**變數 4:**
- Variable name: `KEYSTATIC_GITHUB_TOKEN`
- Value: `ghp_您的GitHubToken`（貼上步驟 1 創建的 Token）
- Environment: 選擇 **"Production"**

#### 3.4 開始部署

1. 確認所有設定都正確
2. 點擊 **"Save and Deploy"** 按鈕
3. 等待構建完成（約 5-10 分鐘）
4. 構建完成後，您會看到一個 URL，例如：`https://actyourplan2026.pages.dev`

---

## ✅ 部署完成後測試

### 基本測試
1. 訪問您的部署 URL
2. 檢查首頁是否正常顯示
3. 測試導航選單
4. 測試語言切換功能

### 登入測試
1. 訪問：`https://您的URL.pages.dev/login`
2. 使用您設定的 `ADMIN_USERNAME` 和 `ADMIN_PASSWORD` 登入
3. 登入成功後，應該能看到 "ID: admin"

### 後台測試
1. 登入後，點擊 **"ACCESS"** 按鈕
2. 應該會進入 Keystatic 後台（`/keystatic`）
3. 嘗試編輯一篇文章，保存
4. 檢查更改是否反映在網站上

---

## 🆘 遇到問題？

### 問題 1: Git 推送失敗

**錯誤**: `remote: Support for password authentication was removed`

**解決**: 使用 GitHub Token 作為密碼（不是 GitHub 帳號密碼）

### 問題 2: 構建失敗

**解決**:
1. 點擊構建日誌，查看錯誤訊息
2. 檢查環境變數是否正確設定
3. 確認所有 4 個環境變數都已添加

### 問題 3: 後台無法訪問

**解決**:
1. 確認 `KEYSTATIC_GITHUB_TOKEN` 環境變數已設定
2. 確認 Token 有效且有 `repo` 權限
3. 重新部署一次

---

## 📝 快速檢查清單

- [ ] GitHub Token 已創建並保存
- [ ] 代碼已推送到 GitHub
- [ ] Cloudflare Pages 專案已創建
- [ ] 4 個環境變數已設定
- [ ] 構建已成功完成
- [ ] 網站可以正常訪問
- [ ] 登入功能正常
- [ ] 後台編輯功能正常

---

## 🎉 完成！

部署完成後，您的網站就可以在網路上訪問了！

**記住保存：**
- GitHub Token
- 管理員密碼
- Cloudflare Pages URL

祝您部署順利！🚀

